package org.essoft.oop

class Mathematics {
    fun sum(): int {
        return 0
    }

    fun sum(x: Int, y: Int) : Int {
        return x + y
    }

    fun sum(x: Int, y: Int, z: Int) : Int {
        return x + y + z
    }
}